#include <stdio.h>

int main(void) {
    int i = 0;
    while (i < 10) {
        printf("%s\n", "Paradise");//can also be done as printf("Paradise\n");
        i = i + 1;
    }
    return 0;
 }